{
  "name": "@academix-admin/domain-types",
  "version": "0.1.13",
  "private": true,
  "description": "Single source of truth for Academix domain types — generated Supabase row types + the RPC/Lambda wire contracts + shared status enums. Consumed across web, lambda, desktop; the canonical input for generating Dart (mobile) models. Git-tracked, not published to npm.",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": { "types": "./dist/index.d.ts", "default": "./dist/index.js" },
      "require": { "types": "./dist/index.d.cts", "default": "./dist/index.cjs" }
    },
    "./database": {
      "import": { "types": "./dist/database.types.d.ts", "default": "./dist/database.types.js" }
    },
    "./package.json": "./package.json"
  },
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsup",
    "prepare": "tsup",
    "clean": "rimraf dist",
    "dev": "tsup --watch",
    "gen:db": "supabase gen types typescript --project-id iewqfmkngcgayxbbnpiz --schema public > src/database.types.ts",
    "gen": "node scripts/gen.mjs",
    "verify": "node scripts/gen.mjs && git diff --exit-code -- src/generated dart/lib/src",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "quicktype": "23.2.6",
    "rimraf": "^5.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  }
}
